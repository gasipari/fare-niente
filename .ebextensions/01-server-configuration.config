files:
  "/etc/data-app/test1.txt":
    mode: "000755"
    owner: root
    group: root
    content: |
      APPLICATION_ENV>DEV

container_commands:
  command block:
    command: cp /etc/data-app/test1.txt /var/app/current
    #test: '[ "${APPLICATION_ENV}" == "DEV" ]'
    ignoreErrors: true